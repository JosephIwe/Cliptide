{
  "name": "@cliptide/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Cliptide desktop host. M1 spike: Electron-native clipboard source only — no window, no tray, no shortcut.",
  "type": "module",
  "main": "src/main.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "probe": "electron --no-sandbox src/probe.js",
    "probe:xvfb": "xvfb-run -a electron --no-sandbox src/probe.js"
  },
  "devDependencies": {
    "electron": "43.3.0"
  },
  "license": "UNLICENSED"
}
