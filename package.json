{
  "name": "cliptide",
  "version": "0.1.0",
  "private": true,
  "description": "Privacy-first, local-first cross-platform clipboard agent. Copy, remember, summon, select, paste.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "demo": "npm run demo --workspace @cliptide/engine"
  },
  "license": "UNLICENSED"
}
